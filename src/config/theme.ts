import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#2FCC59"
        },
        background: {
            default: '#E9E9EB',
            paper: '#D9D9D9',
        },
    }
})

export default theme